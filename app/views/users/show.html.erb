<h1><%= @user.username %>'s Profile</h1>

<div class="col-md-8">

    <h3> Reviews: (<%= @user.reviews.count %>)</h3>

    <% @reviews.each.with_index(1) do |r, i| %>
    <%= "#{i}" %>. <%= r.title %> - <%= r.content %>
    <% end %>

  </div>
</div>
